\documentclass[12pt]{book}

\usepackage{geometry}
\geometry{letterpaper, portrait, margin=.75in, inner=1in, top=1in}

\usepackage{amsmath} % allows for align* mode and many math symbols

%\renewcommand{\familydefault}{\sfdefault}

\usepackage{graphicx} % Required for inserting images
%\usepackage{tikz}
%\usetikzlibrary{positioning}

\usepackage{multicol} % allows to add and remove columns throughout the document

%\usepackage{fmtcount}

%\usepackage{colortbl}

% establish "oddenumerate" for answer key
\usepackage[shortlabels]{enumitem}
\newlist{oddenumerate}{enumerate}{1}
\setlist[oddenumerate]{start=0,label=\theoddenumeratei.}
\makeatletter
\renewcommand\theoddenumeratei{\@arabic{\numexpr2*\value{oddenumeratei}+1}}
\makeatother

% to center in tables for formula notes
\usepackage{array}
\newcolumntype{M}[1]{>{\centering\arraybackslash}m{#1}} 

 % to edit header/footer and page numbering
\pagenumbering{arabic}
\usepackage{fancyhdr}
\fancyhf{}
\renewcommand{\headrulewidth}{0pt}
\fancyfoot[C]{\thepage}
\pagestyle{fancy}

% for long division
\usepackage{scalerel}
\usepackage{stackengine}
\newcommand\showdiv[1]{\overline{\smash{\hstretch{.5}{)}\mkern-3.2mu\hstretch{.5}{)}}#1}}
\let\ph\phantom
\newlength{\dividendlength}
\newlength{\divisorlength}
\newlength{\dividendheight}
\newlength{\divisorheight}
\newlength{\maxheight}

\usepackage{xcolor} % allow lightgray highlighting

\setlength{\parindent}{0pt} % eliminate indentations throughout

\setcounter{tocdepth}{0} % omit "sections" from TOC

 % index for problem numbers vs. ALEKS topic names
\usepackage{imakeidx}
\makeindex[
  columns=1, % can set to 1 or 2 columns
  title={ALEKS Topic Names}
]

% to track problems within each module, and force 2-digit numbering (01, 02, etc.)
\newcounter{problem}
\counterwithin{problem}{chapter}
\makeatletter
\renewcommand\theproblem{\two@digits{\value{problem}}}
\makeatother


\fboxsep=7pt % set margins in the box around each problem


%\usepackage{multicol}
%\setlength{\columnsep}{1cm}

\usepackage{pdfpages} % to allow PDFs

% add headers and page numbers to PDFs
\fancypagestyle{pdfpages}{
  \fancyhf{}
  \fancyfoot[c]{\pdfpagesfootercenter}
  \fancyhead[RO,LE]{\pdfpagesheader}
}
\newcommand*\pdfpagesfootercenter{}
\newcommand*\pdfpagesheader{}

\newcommand{\setlayout}[3]{\thispagestyle{pdfpages}%
  \gdef\pdfpagesfootercenter{#1}%
  \gdef\pdfpagesheader{#2}%
}





%\usepackage{tabularx}

% establish table of contents formatting
\renewcommand*\contentsname{Table of Contents}
\usepackage{tocloft}
\renewcommand{\cftchapleader}{\cftdotfill{\cftdotsep}}


%%%% big section "part" titles

\usepackage{titlesec}

\titleformat
{\partial} % command
[hang] % shape
{\bfseries\LARGE} % format
{Section \ \thechapter} % label
{0.5ex} % sep
{
    \centering
} % before-code

\titleformat{\section}[wrap]
{\normalfont\bfseries}
{\thesection.}{0.5em}{}

\titlespacing{\section}{12pc}{1.5ex plus .1ex minus .2ex}{1pc}

\renewcommand{\partname}{Section}

%%%%%%%% chapter titles

\titleformat
{\chapter} % command
[display] % shape
{\LARGE} % format
{\thechapter :} % label
{0.5ex} % sep
{
    \bfseries
    \hspace{0.5 in}
} % before-code

\renewcommand{\chaptername}{Unit}

%%%%%%%% sub-chapter titles (Notes / Practice / etc.)

\titleformat
{\section} % command
[display] % shape
{\LARGE} % format
{} % label
{0.5ex} % sep
{
    \bfseries
    \hspace{0.5 in}
} % before-code

%%%


\title{Math 100 Class Notes}
\author{The University of Arizona}
\date{Fall 2024}

\fancyhead[RO,LE]{\leftmark}

\setlength{\headheight}{14.5pt}

